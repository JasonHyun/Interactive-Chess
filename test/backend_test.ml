open Backend
open OUnit2

(** Although the functionality cannot be checked directly for non public
    methods, the following set of tests should test all of the backend
    code written so far. Please message Neil/Ved if this is not the
    case.*)
let test_make_pieces
    (player : Backend.player)
    (piece_types : piece_type list) =
  0
